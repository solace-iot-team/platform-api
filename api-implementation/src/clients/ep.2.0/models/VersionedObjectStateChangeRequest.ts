/* eslint-disable */


export type VersionedObjectStateChangeRequest = {
    stateId?: string;
}

export namespace VersionedObjectStateChangeRequest {

    /**
     * the discriminator for the model if required for more complex api's
     */
    export const discriminator = 'VersionedObjectStateChangeRequest';


}