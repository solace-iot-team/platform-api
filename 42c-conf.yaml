audit:
  branches:
    '*':
      discovery:
        search:
          - '**/*.json'
          - '**/*.yaml'
          - '**/*.yml'
          - '!clients/**'
      fail_on:
        invalid_contract: false
