# release tests

````bash

export APIM_RELEASE_TEST_PLATFORM_ADMIN_USER="{admin user}"
export APIM_RELEASE_TEST_PLATFORM_ADMIN_PASSWORD="{admin password}"
export APIM_RELEASE_TEST_SOLACE_CLOUD_URL="{solace-cloud-api-url}"
export APIM_RELEASE_TEST_SOLACE_CLOUD_TOKEN="{solace-cloud-api-token}"
export APIM_RELEASE_TEST_SOLACE_EVENT_PORTAL_URL="{solace-event-portal-api-url}"
export APIM_RELEASE_TEST_SOLACE_EVENT_PORTAL_TOKEN="{solace-event-portal-api-token}"
./run.sh

````

---
